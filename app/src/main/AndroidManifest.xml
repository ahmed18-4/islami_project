<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IslamiProject"
        tools:targetApi="31">
        <activity
            android:name=".ui.HadethDetailsActivity"
            android:exported="false"
            android:label="@string/title_activity_hadeth_details"
            android:theme="@style/Theme.IslamiProject" />

        <activity
            android:name=".ui.SuraDetailsActivity"
            android:exported="false"
            android:label="@string/title_activity_sura_details"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.IslamiProject" />
        <activity
            android:name=".SplachScreen"
            android:exported="true"
            android:theme="@style/noActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/noActionBar" />
    </application>

</manifest>